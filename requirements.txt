# NaturalShell — dependencies
# pip install -r requirements.txt

# LLM provider SDKs — zero LangChain, zero Pydantic issues
groq>=0.9.0                    # Groq (LLaMA 3.3, Mixtral)
google-generativeai>=0.8.0     # Gemini
# Ollama needs no package — uses its REST API directly

# Vector store + local embeddings (no API key needed)
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Output
rich>=13.0.0
